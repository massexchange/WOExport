<h3>{name}</h3>

<div>
    Status:
    <span class="value">{?status}{status}{:else}Unknown{/status}</span>
</div>
<div class="waiting">
    Jobs waiting:
    <span class="value">{?queueSize}{queueSize}{:else}Unknown{/queueSize}</span>
</div>
<input type="button" class="cancelTasks" value="Clear" />
<div class="exceptions">
    Exceptions:
    <span class="value">{?exceptions}{exceptions.length}{:else}0{/exceptions}</span>
</div>
